.push
.wrap
  %h3.section_title Upload your original beat


  = simple_form_for(@user_beat, url: dashboard_index_path) do |f|
    = f.error_notification
    .form-inputs
      = f.file_field :beat
    .form-actions
      = f.button :submit, "Upload", class: 'button_purple'

